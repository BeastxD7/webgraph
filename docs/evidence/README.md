# Evidence

Screenshots and small artefacts referenced from pull requests, kept in the repository so a
PR's "before / after" survives the browser tab that took it. One folder per PR topic; the
PR description says which file shows what. Nothing here is loaded by the code.
