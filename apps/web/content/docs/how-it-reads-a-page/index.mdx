---
title: How it reads a page
description: Fetching, the union of static and rendered DOM, reading order recovery, and Markdown rendering.
---

A page is fetched twice, merged, measured in a browser, ordered by its layout and rendered to Markdown. Each stage, what it produces, and what it does when something goes wrong.

<Callout title="Placeholder">
  This section is being written. Its pages arrive in the same pull request series that
  added the documentation site.
</Callout>
