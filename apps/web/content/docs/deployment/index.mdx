---
title: Deployment
description: Running the API and the web app in production.
---

The API ships as a container to Cloud Run (`make deploy-api`); the web app deploys to Vercel (`make deploy-web`). This section covers both and the configuration they need.

<Callout title="Placeholder">
  This section is being written. Its pages arrive in the same pull request series that
  added the documentation site.
</Callout>
