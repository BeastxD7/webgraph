---
title: Getting started
description: Install webgraph, start the API and the web app, and extract a first site.
---

One `make install` sets up the Python engine, the API and the Next.js app; `make api` and `make web` run them on ports 8000 and 3000.

<Callout title="Placeholder">
  This section is being written. Its pages arrive in the same pull request series that
  added the documentation site.
</Callout>
